environments {
   default {}

}

serenity {
  project.name = "User Blog API Tests"
  take.screenshots = FOR_FAILURES
  logging = "NORMAL"
  feature.file.encoding = UTF-8
  report.encoding = UTF-8
  logging = VERBOSE
  console.colors = true
  serenity.timeout = 5000
  thucycides.step.delay = 500
  feature.file.encoding = UTF-8
  requirement.types = "epic, feature"
  api.environment = "SIT"
}